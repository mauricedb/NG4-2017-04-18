import { BlaDirective } from './bla.directive';

describe('BlaDirective', () => {
  it('should create an instance', () => {
    const directive = new BlaDirective();
    expect(directive).toBeTruthy();
  });
});
